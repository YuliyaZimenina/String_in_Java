// Example of using a text block
public class TextBlock {
    public static void main(String[] args) {
        String textBlock = """
                Text blocks make it easier to work with multiple strings because they
                   eliminate the need to use escape sequences to indicate a new line.
                As a result, text blocks make the programmer's job easier!
                """;
        System.out.println(textBlock);
    }
}
